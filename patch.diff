--- src/components/ui/ImageGallery.astro
+++ src/components/ui/ImageGallery.astro
@@ -48,12 +48,15 @@
     { eager: false },
   );

+  const folderPath = `/${folder}/`;
+  const folderPathLen = folderPath.length;
+
   const filteredImages = Object.entries(allImages).filter(([path]) => {
-    const folderMatch = path.includes(`/${folder}/`);
-    if (!folderMatch) return false;
+    const folderMatchIndex = path.indexOf(folderPath);
+    if (folderMatchIndex === -1) return false;

-    const isInSubfolder = path.split(`/${folder}/`)[1]?.includes("/");
-    if (isInSubfolder) return false;
+    const isInSubfolder = path.indexOf("/", folderMatchIndex + folderPathLen) !== -1;
+    if (isInSubfolder) return false;

     for (const pattern of excludePatterns) {
       if (path.includes(pattern)) return false;

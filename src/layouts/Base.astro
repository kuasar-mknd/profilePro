---
import SeoHead from "../components/common/SeoHead.astro";
import LocalBusinessSchema from "../components/common/LocalBusinessSchema.astro";
import Header from "../components/common/Header.astro";
import Footer from "../components/common/Footer.astro";
import Lightbox from "../components/ui/Lightbox.astro";
import "../css/style.css";
import "@fontsource/inter";
import "@fontsource/space-grotesk";
import "@fontsource/outfit";

import { ClientRouter } from "astro:transitions";

const { postData, jsonLd, noPadding = false, lang = "fr" } = Astro.props;
---

<!doctype html>
<html lang={lang} class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <style>
      @media (prefers-reduced-motion: reduce) {
        :global(html) {
          scroll-behavior: auto !important;
        }
      }
    </style>
    <!-- Critical preconnects - must be early in head for effectiveness -->
    <link rel="dns-prefetch" href="https://unpkg.com" />
    <link rel="preconnect" href="https://unpkg.com" crossorigin />
    <link rel="dns-prefetch" href="https://api.websitecarbon.com" />
    <link rel="preconnect" href="https://api.websitecarbon.com" crossorigin />
    <link
      rel="preconnect"
      href="https://static.cloudflareinsights.com"
      crossorigin
    />
    <meta name="theme-color" content="#f8fafc" />
    <script is:inline>
      // Early theme detection to prevent FOUC
      if (
        localStorage.getItem("theme") === "dark" ||
        (!localStorage.getItem("theme") &&
          window.matchMedia("(prefers-color-scheme: dark)").matches)
      ) {
        document.documentElement.classList.add("dark");
      }
    </script>
    <SeoHead postData={postData} jsonLd={jsonLd} />
    <LocalBusinessSchema />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="manifest" href="/manifest.json" />
    <ClientRouter />
  </head>
  <body
    class="bg-pacamara-white dark:bg-pacamara-dark transition-colors duration-300 min-h-screen flex flex-col"
  >
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 z-9999 px-6 py-3 bg-white text-pacamara-primary font-bold rounded-full shadow-lg border border-pacamara-secondary"
    >
      Aller au contenu principal
    </a>
    <Header />

    <main
      id="main-content"
      class={`grow ${noPadding ? "" : "pt-24"}`}
      transition:animate="fade"
      tabindex="-1"
    >
      <slot />
    </main>

    <Footer />

    <!-- Lightbox at body level for proper fixed positioning -->
    <Lightbox />

    <!-- Back to Top Button -->
    <button
      id="back-to-top"
      class="fixed bottom-6 right-6 z-50 p-3 rounded-full bg-pacamara-secondary/90 text-white shadow-lg hover:bg-pacamara-accent transition-all duration-300 opacity-0 invisible translate-y-4 hover-lift"
      aria-label="Retour en haut de page"
      tabindex="-1"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="h-6 w-6"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
        aria-hidden="true"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M5 10l7-7m0 0l7 7m-7-7v18"></path>
      </svg>
    </button>

    <script>
      function initBackToTop() {
        const backToTopBtn = document.getElementById("back-to-top");

        if (backToTopBtn) {
          // Show button when scrolling down
          const toggleButton = () => {
            if (window.scrollY > 300) {
              backToTopBtn.classList.remove(
                "opacity-0",
                "invisible",
                "translate-y-4",
              );
              backToTopBtn.classList.add("visible", "translate-y-0");
              backToTopBtn.setAttribute("tabindex", "0");
            } else {
              backToTopBtn.classList.add(
                "opacity-0",
                "invisible",
                "translate-y-4",
              );
              backToTopBtn.classList.remove("visible", "translate-y-0");
              backToTopBtn.setAttribute("tabindex", "-1");
            }
          };

          window.addEventListener("scroll", toggleButton, { passive: true });

          // Initial check
          toggleButton();

          // Scroll to top action
          backToTopBtn.addEventListener("click", () => {
            window.scrollTo({ top: 0, behavior: "smooth" });
          });
        }
      }

      // Initialize on load and after view transitions
      initBackToTop();
      document.addEventListener("astro:after-swap", initBackToTop);

      // Update theme-color meta tag based on current theme
      function updateThemeColor() {
        const isDark = document.documentElement.classList.contains("dark");

        // We can dynamically update one specific meta tag if needed, or rely on the media attribute.
        // However, some browsers don't respect the media attribute on theme-color dynamically.
        // A more robust way is to update a single meta tag or ensuring the browser picks up the change.
        // For simple switching, the existing media query approach works for OS preference changes.
        // But for manual toggles, we might need to force it.

        const meta = document.querySelector('meta[name="theme-color"]');
        if (meta) {
          meta.setAttribute("content", isDark ? "#0b0f19" : "#f8fafc");
        }
      }

      // Observer for class changes on html to trigger theme color update
      const themeObserver = new MutationObserver((mutations) => {
        mutations.forEach((mutation) => {
          if (mutation.attributeName === "class") {
            updateThemeColor();
          }
        });
      });

      themeObserver.observe(document.documentElement, { attributes: true });

      // 🛡️ Sentinel: Prevent Reverse Tabnabbing
      // Ensures all external links opening in a new tab have rel="noopener noreferrer"
      function secureLinks() {
        const links = document.querySelectorAll('a[target="_blank"]');
        links.forEach((link) => {
          const rel = link.getAttribute("rel") || "";
          if (!rel.includes("noopener")) {
            link.setAttribute("rel", (rel + " noopener noreferrer").trim());
          }
        });
      }

      // Run on load and after view transitions
      secureLinks();
      document.addEventListener("astro:after-swap", secureLinks);
    </script>
  </body>
</html>

---
import dateFormat, { masks } from "dateformat";
const { date } = Astro.props;
---

<small
  class="text-pacamara-primary/70 font-pacamara-space tracking-widest transition-all duration-300 text-[16px]
        dark:text-white/60"
>
  {dateFormat(date, "dd-mm-yyyy")}
</small>

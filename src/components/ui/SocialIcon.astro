---
import { Icon } from "astro-icon/components";

interface Props {
  socialUrl?: string;
  socialType: string;
}

const { socialUrl = false, socialType } = Astro.props;
const capitalizedType =
  socialType.charAt(0).toUpperCase() + socialType.slice(1);
const label = `Visitez notre page ${capitalizedType} (ouvre un nouvel onglet)`;
---

{
  socialUrl && (
    <li>
      <a
        href={socialUrl}
        target="_blank"
        rel="noopener noreferrer"
        aria-label={label}
        title={label}
      >
        <Icon
          name={socialType}
          class="w-auto h-[15px] transition-all duration-300 fill-pacamara-primary/60 hover:fill-pacamara-secondary dark:fill-pacamara-accent dark:hover:fill-pacamara-secondary"
          aria-hidden="true"
        />
      </a>
    </li>
  )
}

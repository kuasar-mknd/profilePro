---
import { Icon } from "astro-icon/components";

interface Props {
  icon: string;
  url: string;
  label: string;
}

import { isValidUrl } from "../../utils/security";

const { icon, url, label } = Astro.props;

// 🛡️ Sentinel: Validate URL protocol to prevent XSS (javascript: URI)
// Allow http, https, mailto, and relative paths (/)
const safeUrl = isValidUrl(url, { allowMailto: true }) ? url : "#";

// Palette: Accessibility enhancement
// Append warning for external links (which open in new tab)
const suffix = " (ouvre un nouvel onglet)";
const baseLabel = label || "Lien social";
const a11yLabel = baseLabel.includes(suffix) ? baseLabel : baseLabel + suffix;
---

<a
  href={safeUrl}
  target="_blank"
  rel="noopener noreferrer"
  class="group relative flex h-10 w-10 items-center justify-center rounded-full bg-pacamara-primary/5 transition-all duration-300 hover:scale-110 focus-visible:scale-110 hover:bg-pacamara-accent focus-visible:bg-pacamara-accent focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-pacamara-accent focus-visible:ring-offset-2 dark:bg-pacamara-white/10 dark:hover:bg-pacamara-accent"
  style="contain: layout style;"
  aria-label={a11yLabel}
  data-no-warning-title="true"
>
  <Icon
    name={icon}
    class="h-5 w-5 text-pacamara-primary transition-colors duration-300 group-hover:text-white dark:text-pacamara-white"
  />
  <span class="sr-only">{a11yLabel}</span>

  <!-- 🎨 Palette: Custom Tooltip -->
  <span
    class="absolute bottom-full mb-2 left-1/2 -translate-x-1/2 px-2 py-1 bg-pacamara-dark text-white text-xs font-medium rounded opacity-0 group-hover:opacity-100 group-focus-visible:opacity-100 transition-opacity duration-300 pointer-events-none whitespace-nowrap z-10 shadow-sm"
    aria-hidden="true"
  >
    {label}
  </span>
</a>
